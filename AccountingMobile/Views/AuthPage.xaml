<?xml version="1.0"
    encoding="utf-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:entryField="clr-namespace:AccountingMobile.CustomControls.EntryField"
             x:Class="AccountingMobile.Views.AuthPage">
    <ContentPage.Content>
        <VerticalStackLayout Spacing="20"
                             Padding="20">
        <entryField:EntryField Placeholder="Логін"/>
        <entryField:EntryField Placeholder="Пароль"/>
        <Button Text="Увійти"
                Command="{Binding LoginCommand}"/>
            
        </VerticalStackLayout>
    </ContentPage.Content>
</ContentPage>